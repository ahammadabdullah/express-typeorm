meta {
  name: Create todo
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/todos
  body: json
  auth: inherit
}

body:json {
  {
    "title": "Buy groceries 2",
    "desc": "Milk, eggs, bread"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
